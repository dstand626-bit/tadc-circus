---
description: 'A tadc-app app.'
labels: ['app', 'react']
---

A React application for tadc-app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use tadc-app 
```

To run the app locally, run the following:

```
bit run tadc-app 
```
